# build artifacts -- everything in the repo is source and the kernel, the
# ISO and every seed are rebuilt from it
kmain.s
*.o
moonshot.elf
*.iso
isoroot*/
*.log
*.sock
*.ppm
*.bak
*.panicbak
*.dfbak
disk.img
# written by build.sh after a passing bootstrap audit; describes a binary on
# this machine, not source (see README, Trust)
coff1.sha256
